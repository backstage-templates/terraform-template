name: Teste Build Template

on:
  push:
    branches: [main]

# Os jobs que irão rodar
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
